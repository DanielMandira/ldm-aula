import { StyleSheet } from "react-native";

export default StyleSheet.create({
    button: {
      width: 60,
      height: 60,
      borderRadius: 30,
      justifyContent: 'center',
      alignItems: 'center',
      margin: 5,
    },
    selected: {
      borderWidth: 2,
      borderColor: '#fff',
    },
    text: {
      fontSize: 24,
      color: '#fff',
      fontWeight: 'bold',
    },
  }); 